# Зависимости приложения — с прибитыми версиями, чтобы окружение собиралось
# одинаково и результат не «плыл» от выхода новых версий пакетов.
#
# Установка:   python -m pip install -r requirements.txt
# Обновление:  поменять версию здесь (или снять текущее окружение:
#              python -m pip freeze) и снова выполнить установку.
#
# Сверху — прямые зависимости: то, что приложение импортирует само.
# Ниже — их транзитивные зависимости: их можно не читать, pip поставит их сам.

# Веб-сцена и живое обновление ленты: сервер и WebSocket
flask==3.1.3
flask-socketio==5.6.1

# Разметка реплик: markdown -> HTML. Формулы при этом уходят отдельным элементом
markdown==3.10.3

# Точный подсчёт токенов. Без него всё работает — оценка становится приблизительной
tiktoken==0.14.0

# Поиск в интернете: факты в репликах и картинки для аватаров
ddgs==9.16.0

# --- Транзитивные зависимости (ставятся автоматически) ---
bidict==0.24.1
blinker==1.9.0
certifi==2026.7.22
charset-normalizer==3.5.1
click==8.5.0
h11==0.16.0
idna==3.19
itsdangerous==2.2.0
Jinja2==3.1.6
lxml==6.1.3
MarkupSafe==3.0.3
primp==2.0.0
python-engineio==4.14.0
python-socketio==5.16.4
regex==2026.9.10
requests==2.34.2
simple-websocket==1.1.0
typing_extensions==4.16.0
urllib3==2.7.0
Werkzeug==3.1.8
wsproto==1.3.2
