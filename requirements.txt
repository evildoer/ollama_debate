# Прямые зависимости приложения, без версий: обновление обычное
# (python -m pip install --upgrade -r requirements.txt), а точный снимок
# рабочего окружения лежит рядом в pip.freeze.txt — он для сверки, не для установки.
#
# Транзитивные зависимости (werkzeug, python-socketio, jinja2 и прочие) ставить
# вручную не нужно — pip притянет их сам.

# Веб-сцена и живое обновление ленты: сервер и WebSocket
flask
flask-socketio

# Разметка реплик: markdown -> HTML. Формулы при этом уходят отдельным элементом
markdown

# Точный подсчёт токенов. Без него всё работает — оценка становится приблизительной
tiktoken

# Поиск в интернете: факты в репликах и картинки для аватаров
ddgs
